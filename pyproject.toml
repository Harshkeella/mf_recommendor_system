[project]
name = "mf-recommendor-system"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.0",
    "numpy>=1.24",
    "scikit-learn>=1.3",
    "SQLAlchemy>=2.0",
    "fastapi>=0.110",
    "pydantic>=2.0",
]

[tool.vercel]
entrypoint = "app.api:app"
